
chown "name" "filename" 更改文件或目录属于哪个用户
chown :"name" "filename" 更改文件或目录的用户组

echo "hello lose" | tr a-zA-Z n-za-mN-ZA-M   文本模糊处理(简单加密)

在/usr/local/sbin 下编写脚本，可以直接输入文件名执行脚本，不需要再指定路径，如 ls

du -sh 查看当前文件或目录大小

sed 文本替换命令
	sed 's/要替换的文本/替换后的文件/' "文件名"		替换每一行的第一处匹配的文本, 's/../../g' 全局替换, 这个命令是替换后输出，没有修改源文件
	sed -i 's/要替换的文本/替换后的文件/' "文件名"  输出替换后的内容，并且修改源文件为替换后的
	
	sed '/^$/d' "文件名"  移除空白行
